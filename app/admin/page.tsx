import { AdminPanel } from "@/components/admin/admin-panel";

export const dynamic = "force-dynamic";
export const metadata = { title: "Nogthy Store - Painel Administrativo" };

export default function AdminPage() {
  return <AdminPanel />;
}
